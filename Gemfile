source :gemcutter

gem 'rails',               '3.0.9'

# bundler requires these gems in all environments
gem 'aasm', '2.3.1'
gem 'acts_as_tree'
gem 'awesome_print'
gem 'compass'
gem 'fastercsv'
gem 'haml'
gem 'mysql2',              '0.2.6' # <--- for Rails 3.0!!!
gem 'nokogiri'
gem 'paperclip'
gem 'paper_trail'
gem 'ruport',              '1.6.3'
gem 'acts_as_reportable'
gem 'will_paginate'
gem 'jquery-rails',        '>= 1.0.12'

group :development do
  # bundler requires these gems in development
  # gem 'rails-footnotes'
end

group :test do
  # bundler requires these gems while running tests
  # gem 'rspec'
  # gem 'faker'
end

